# CyberSource Merged Spec — .NET SDK

[![Built with APIMatic](https://www.apimatic.io/hubfs/Built-with-APIMatic-badge.svg)](https://www.apimatic.io) [![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](https://licenses.nuget.org/MIT)

The CyberSource Merged Spec SDK for .NET provides access to the CyberSource Merged Spec REST APIs
from .NET applications. All CyberSource API specs merged together; the underlying APIs are
documented at <https://developer.cybersource.com/api/reference/api-reference.html>.

> Generated by [APIMatic](https://www.apimatic.io). This is a community package and is not an
> official CyberSource or Visa release.

---

## Installation

```bash
dotnet add package APIMatic.VisaCyberSource
```

Targets `netstandard2.0`, so it runs on .NET Framework 4.6.1+, .NET Core 2.0+, and all modern .NET.

---

## Quick Start

### Dependency Injection

Register the client with `IServiceCollection` and resolve it from the container. The `HttpClient`
is managed by `IHttpClientFactory`. Configure the client's behavior through
`CyberSourceMergedSpecClientOptions`.

```csharp
using Microsoft.Extensions.DependencyInjection;
using CyberSourceMergedSpec;
using CyberSourceMergedSpec.Servers;

services.AddCyberSourceMergedSpecClient(options =>
    {
        options.Environment = ServerEnvironment.Production;
        // TODO: configure more client options here
    });
```

### Direct Instantiation

Create the client by passing an `HttpClient` you manage yourself.

```csharp
using System.Net.Http;
using CyberSourceMergedSpec;
using CyberSourceMergedSpec.Servers;

var httpClient = new HttpClient();
var options =
    new CyberSourceMergedSpecClientOptions
    {
        Environment = ServerEnvironment.Production,
    };
var client = new CyberSourceMergedSpecClient(httpClient, options);
```

---

## Best Practices

> Use a **single `CyberSourceMergedSpecClient` instance** for the lifetime of your application and
> reuse it across all requests. Creating a new instance per request might exhaust the connection
> pool.

---

## Documentation

This SDK exposes **135 operations**. Full per-operation documentation — runnable code samples,
per-parameter descriptions, error responses — ships with the SDK source distribution as
`api-reference.md`, alongside a generated machine-readable **SDK map** (`sdk-map.md` and `map/`)
that indexes the entire C# surface: exact signatures, return types, error accessors, and the
source file behind each type.

The package itself ships XML documentation, so signatures and parameter docs are available through
IntelliSense.

---

## License

Distributed under the [MIT License](https://licenses.nuget.org/MIT).
